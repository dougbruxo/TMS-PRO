---
description: Como iniciar o servidor de desenvolvimento para preview do projeto.
---

1. Iniciar o servidor local:
   ```bash
   // turbo
   npm run dev
   ```

2. Após o comando iniciar, o terminal mostrará a URL (ex: http://localhost:9002). Abra essa URL no navegador para testar a aplicação.
